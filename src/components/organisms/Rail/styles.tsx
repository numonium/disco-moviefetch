import { styled } from 'styled-components';

export const Rail = styled.div`
  overflow: hidden;
  overflow-x: auto;
  width: 100%;
  margin: 0 auto 20px;
`;

export const RailItems = styled.ul`
  margin: 0 auto;
  padding: 0 2vmax;
  flex-direction: row;
  list-style: none;
  text-align: center;
`;
